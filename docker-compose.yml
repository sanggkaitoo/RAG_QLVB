services:
  qdrant:
    image: qdrant/qdrant:v1.9.2
    container_name: qlvb_qdrant
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - ./data/qdrant:/qdrant/storage
    environment:
      - QDRANT__SERVICE__API_KEY=${QDRANT_API_KEY:-changeme_qdrant_key}
